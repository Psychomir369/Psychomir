from .cart import Cart

# را به کانتکست تمامی ویوها اضافه می‌کند (Cart) این فایل یک کانتکست پروسسور تعریف می‌کند که به صورت خودکار سبد خرید
# قابل دسترسی خواهد بود (Templates) به این ترتیب، سبد خرید در تمامی قالب‌ها 

def cart(request):
    return {'cart': Cart(request)}  # سبد خرید جاری را به کانتکست اضافه می‌کند.
